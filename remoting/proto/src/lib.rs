//! Generated tonic/prost types for `pkcs11_remote.proto`. Both the server
//! (`remoting/grpc`) and the bench-harness gRPC client (`rust/bench-harness`)
//! depend on this crate so the wire schema is single-sourced — see
//! `proto/pkcs11_remote.proto` for the schema itself.

tonic::include_proto!("pqctoday.pkcs11remote.v1");
