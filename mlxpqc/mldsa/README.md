# mlxpqc/mldsa — ML-DSA (Dilithium / FIPS-204) GPU port

Forking the **pq-crystals/dilithium** reference and translating it to Metal, guided
by the validated findings in [../bench/](../bench/) and [../TEST_PLAN.md](../TEST_PLAN.md).

## Legal basis (important)

- **Base = `ref/`** — pq-crystals/dilithium reference C, **Public Domain (CC0) /
  Apache-2.0 / GPL-2.0**. Freely forkable, modifiable, redistributable. This is the
  implementation FIPS-204 is built on.
- **Apple corecrypto (`../../arm/`) is NOT in this repository** (gitignored; license
  forbids redistribution — download from <https://github.com/apple/corecrypto>, branch
  `2026-05`, to use it as a local oracle). See [../NOTICE.md](../NOTICE.md). It is NOT forked: its license permits reading
  *only to verify correctness*; redistribution / derivative works are forbidden.
  It is used solely as a read-only correctness oracle (KAT comparison).
- You do **not** "recompile C for GPU" — you translate the reference algorithm into
  Metal kernels. The C is the blueprint + a CPU oracle; the kernels are a rewrite.

## Layout

- `ref/` — forked pq-crystals reference C (the blueprint + CPU oracle).
- `gpu/refgen/` — small C harnesses (built against `ref/`) that dump
  deterministic reference fixtures to `/tmp/mldsa_*.bin` for the Swift
  kernels below to diff against (`ref_harness`, `sign_harness`, `sk_harness`,
  plus `bench*.c` throughput probes). Build each with
  `cc -DDILITHIUM_MODE=3 -O2 -I../../ref -o <name> <name>.c ../../ref/*.c`.
- `gpu/MLDSANTT.swift` — forward NTT + INTT + pointwise multiply, validated
  bit-exact vs `ref/`, with CPU-1core / CPU-allcore / GPU crossover timing.
- `gpu/MLDSAMATVEC.swift` — matvec (`A·s1hat` + `invntt_tomont` + `s2`) +
  `caddq` + `power2round`, validated vs reference.
- `gpu/MLDSASAMPLE.swift` — ExpandA (`poly_uniform`/`rej_uniform`) and
  ExpandS (`poly_uniform_eta`/`rej_eta`), validated vs reference.
- `gpu/MLDSASHAKE.swift` — SHAKE128/256 kernel (empty-input KATs +
  ExpandA/ExpandS-shaped instances), validated vs reference.
- `gpu/MLDSASIGNPRIM.swift` — `decompose`/`make_hint`/`use_hint`,
  `SampleInBall`, `ExpandMask`, validated vs reference.
- `gpu/MLDSAKEYGEN.swift` — full 8-kernel ML-DSA-65 keygen, pk/sk bit-exact
  vs reference.
- `gpu/MLDSASIGN.swift` — full deterministic Sign (`rnd=0`), bit-exact vs
  reference.
- `gpu/MLDSAVERIFY.swift` — full Verify (valid-signature acceptance +
  tampered-signature rejection), vs reference.

Each file's header comment carries its own `swiftc -O <file>.swift -o
<binary> -framework Metal -framework Foundation` build line and the
`/tmp/mldsa_*.bin` fixture it expects (generated by the matching
`gpu/refgen/` harness — see each file's header for which one).

## Validated design decisions (from ../bench on M5 Max)

| Dilithium component | ref file | GPU approach (validated) |
|---|---|---|
| SHAKE / Keccak (sampling, H) | fips202.c | **scalar per-thread** — cooperative loses ~9× on Apple (no 64-bit simd_shuffle) |
| NTT / INTT | ntt.c | merged/threadgroup, **register-resident**; barrier reduction gave little (M2) |
| modular reduction | reduce.c | Montgomery (reduce.c constants); Barrett ≈ hw-divide on M5 (M4) — keep it simple, constant-time |
| poly / polyvec layout | poly*.c | SIMD-group coalesced batch (M1, ~2.7×) |
| rejection sampling (rej_uniform/eta) | poly.c | ballot + prefix-sum (M6, ~1.5×) + early-abort ordering |
| matrix expand + multiply | poly.c/polyvec.c | on-the-fly column-major + fusion (M7, to build) |
| batch I/O | — | unified memory, zero-copy (M8, 2×); no host blit |
| sign rejection-loop scheduling | sign.c | **static dispatch** — GPU work-queue lost (M10) at tested skew |

## Status

**The full ML-DSA-65 pipeline (keygen → sign → verify) is implemented in
Metal and validated bit-exact against the pq-crystals reference** — all 8
kernel files below, each independently re-run and confirmed PASS on
2026-09-01 (Apple M5 Max):

- [x] Fork legal base (pq-crystals/dilithium)
- [x] **Forward NTT + INTT (invntt_tomont) + pointwise multiply** —
      [`MLDSANTT.swift`](gpu/MLDSANTT.swift), bit-exact vs reference
      (2026-06-13); GPU beats all CPU cores ~8–15× for batched NTT at this
      session's batch sizes, crossover immediate (≥256).
- [x] **Montgomery reduce / power2round** —
      [`MLDSAMATVEC.swift`](gpu/MLDSAMATVEC.swift) (matvec `t = A·s1hat`
      + `invntt_tomont` + `s2`, `caddq` + `power2round` → `(t1, t0)`),
      bit-exact vs reference.
- [x] **SHAKE256/128 kernel** — [`MLDSASHAKE.swift`](gpu/MLDSASHAKE.swift),
      SHAKE128/256 empty-input KATs plus ExpandA/ExpandS-shaped instances
      all PASS; GPU throughput up to ~300× CPU-allcore at large batch.
- [x] **Rejection sampling (rej_uniform / rej_eta)** —
      [`MLDSASAMPLE.swift`](gpu/MLDSASAMPLE.swift) (ExpandA/ExpandS, fused
      SHAKE+rejection), bit-exact vs reference.
- [x] **decompose / make_hint / use_hint / SampleInBall / ExpandMask** —
      [`MLDSASIGNPRIM.swift`](gpu/MLDSASIGNPRIM.swift), all PASS.
- [x] **Assembled keygen → sign → verify** —
      [`MLDSAKEYGEN.swift`](gpu/MLDSAKEYGEN.swift) (8-kernel keygen, pk/sk
      bit-exact), [`MLDSASIGN.swift`](gpu/MLDSASIGN.swift) (deterministic
      sign, `rnd=0`, host-driven rejection loop, 2000/2000 bit-exact),
      [`MLDSAVERIFY.swift`](gpu/MLDSAVERIFY.swift) (2000/2000 valid sigs
      accepted, tampered signature correctly rejected).
- [ ] Validate vs NIST ACVP / corecrypto KATs (validation so far is vs the
      pq-crystals reference only, not yet the ACVP vectors or the
      corecrypto oracle mentioned in [`../NOTICE.md`](../NOTICE.md))
- [ ] Constant-time review (ISA inspection)
- [ ] Re-run crossover on M4 Pro (this session's re-verification ran on
      M5 Max only)

Each kernel is built and run individually per the `swiftc` command in its
own file header; see [Layout](#layout) below. This checklist previously
undercounted the shipped work — the kernel files above were all committed
alongside the forward-NTT kernel on 2026-06-13/17 but the checklist was
never updated to match.

## Method

Every kernel is validated **bit-exact against `ref/`** (faithful CPU port) before any
perf claim, and ultimately against the NIST ACVP vectors and the corecrypto oracle.
Build incrementally: one building block at a time, each validated, then composed.
